[package]
name = "brsc"
version = "0.1.0"
edition = "2021"

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
debug = false
overflow-checks = false
incremental = false

[dependencies]
clap = { version = "4.5.22", features = ["derive"] }
meval = "0.2.0"
